<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mastercooker" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cooker"
        android:label="@string/app_name"
        android:theme="@style/ToolBar" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            </activity>
        <activity
            android:name=".MessageActivity"
            android:label="@string/title_activity_message" >
        </activity>

        <!--
        <provider
            android:name=".database.CookerContentProvider"
            android:authorities="com.mastercooker.mastercooker"
            android:enabled="true"
            android:exported="true" >
        </provider>
        -->

        <activity
            android:name=".FoodListActivity"
            android:label="@string/title_activity_food_list" >
        </activity>
        <activity
            android:name=".FullActivity"
            android:icon="@mipmap/ic_cooker"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
